function loadOrigenes() {
    // Obtener el valor del campo de fecha
    let fechaSel = document.getElementById('fecha').value;
    // Mostrar la alerta con el valor seleccionado
    alert('Fecha seleccionada: ' + fechaSel);
}

